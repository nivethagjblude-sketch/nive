# Secrets - never commit these
.env
.env.*
*.pem
credentials.json
token.json

# Node
node_modules/
.wrangler/

# Leftovers from the old python prototype (kept out just in case)
__pycache__/
*.pyc
.venv/

# NOTE: data/ and docs/data/ are intentionally COMMITTED - the GitHub Pages
# dashboard serves docs/data/ as static JSON, and the weekly workflow commits
# regenerated data back to the repo.